apiVersion: apps/v1
kind: Deployment
metadata:
 name: booking-db
 labels: 
  name: booking-db
  app: mentorplatform

spec:
 replicas: 1  
 selector:
  matchLabels:
   name: booking-db
   app: mentorplatform
 template:
  metadata:
   labels:
    name: booking-db
    app: mentorplatform
  spec:
   containers:
    - name: booking-db  
      image: mcr.microsoft.com/mssql/server:2022-latest
      env:
       - name: ACCEPT_EULA
         value: "Y"
       - name: MSSQL_SA_PASSWORD
         value: Book1!pass
      ports:
       - containerPort: 5000