apiVersion: v1
kind: Service
metadata:
 name: identity_db
 labels: 
  name: identitydb-service
  app: mentorplatform

spec:  
 ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
 selector:
  name: identity_db
  app: mentorplatform