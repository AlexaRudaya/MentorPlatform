apiVersion: apps/v1
kind: Deployment
metadata:
 name: identity_db
 labels: 
  name: identity_db
  app: mentorplatform

spec:
 replicas: 1  
 selector:
  matchLabels:
   name: identity_db
   app: mentorplatform
 template:
  metadata:
   labels:
    name: identity_db
    app: mentorplatform
  spec:
   containers:
    - name: identity_db  
      image: postgres:latest
      env:
       - name: POSTGRES_USER
         value: postgres
       - name: POSTGRES_PASSWORD
         value: password
      ports:
       - containerPort: 5432