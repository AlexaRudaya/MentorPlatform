apiVersion: v1
kind: Service
metadata:
 name: chat_db
 labels: 
  name: chatdb-service
  app: mentorplatform

spec:  
 ports:
  - protocol: TCP
    port: 5001
    targetPort: 1433
 selector:
  name: chat_db
  app: mentorplatform